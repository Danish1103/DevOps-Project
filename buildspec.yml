version: 0.2

phases:
  install:
    commands:
      - curl -o packer.zip https://releases.hashicorp.com/packer/1.8.5/packer_1.8.5_linux_amd64.zip
      - unzip packer.zip
      - sudo mv packer /usr/local/bin/
  build:
    commands:
      - packer init .
      - packer build .

      AWS_ACCESS_KEY : AKIATJX52HFAH3ZTXXIB
AWS_SECRET_KEY : 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

artifacts:
  files:
    - '*'
